using UnityEngine;
using System.Collections;

public class CU_Key : MonoBehaviour {
	/// should have a key mapped to platform
    /// input logic can be done here?
    /// on this class, instead of the platform class
    /// 
    /// wrapper class
    /// 

    public override string ToString() {
        return base.ToString();
    }

    public KeyCode key = KeyCode.A;
}
